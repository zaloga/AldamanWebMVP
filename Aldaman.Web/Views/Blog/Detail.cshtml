@model Aldaman.Web.ViewModels.BlogPostViewModel
@{
    ViewData["Title"] = Model.Post.Title;
}

@section Styles {
    <link rel="stylesheet" href="~/lib/quill/quill.snow.css" />
}

<div class="py-8 md:py-12">
    <div class="mb-8">
        <a asp-action="Index" class="inline-flex items-center text-sm font-medium text-slate-500 dark:text-slate-400 hover:text-slate-900 dark:hover:text-white transition-all">
            <i class="bi bi-arrow-left mr-2"></i>
            @Localizer[UIResourceKeys.AllArticles]
        </a>
    </div>

    <article class="max-w-4xl mx-auto">
        <header class="text-center border-b border-slate-200 dark:border-white/10 pb-8 mb-8">
            <h1 class="text-4xl font-extrabold text-slate-900 dark:text-white tracking-tight sm:text-5xl mb-4">@Model.Post.Title</h1>
            <p class="text-xl text-slate-500 dark:text-slate-400">@Model.Post.Perex</p>
        </header>

        <div class="prose prose-lg dark:prose-invert prose-slate text-slate-700 dark:text-slate-300 mx-auto">
            <div class="ql-snow">
                <div class="ql-editor p-0">
                    @if (!string.IsNullOrEmpty(Model.Post.BodyHtml))
                    {
                        @Html.Raw(Model.Post.BodyHtml)
                    }
                    else
                    {
                        <p class="text-slate-400 italic">@Localizer[UIResourceKeys.NoHtmlContent]</p>
                    }
                </div>
            </div>
        </div>
        
        <footer>
            <div class="flex flex-row justify-between items-center gap-4 mt-8 pt-8 border-t border-slate-200 dark:border-white/10">
                <div>
                    @if (Model.PreviousPost != null)
                    {
                        <a asp-action="Detail" asp-route-slug="@Model.PreviousPost.Slug" class="inline-flex items-center text-sm font-medium text-slate-500 dark:text-slate-400 hover:text-slate-900 dark:hover:text-white transition-all">
                            <i class="bi bi-arrow-left mr-2"></i>
                            @Localizer[UIResourceKeys.PreviousPost]
                        </a>
                    }
                </div>
                <div>
                    @if (Model.NextPost != null)
                    {
                        <a asp-action="Detail" asp-route-slug="@Model.NextPost.Slug" class="inline-flex items-center text-sm font-medium text-slate-500 dark:text-slate-400 hover:text-slate-900 dark:hover:text-white transition-all">
                            @Localizer[UIResourceKeys.NextPost]
                            <i class="bi bi-arrow-right ml-2"></i>
                        </a>
                    }
                </div>
            </div>
        </footer>
    </article>
</div>
